package stack_queue

/**
  * A LockFreeExchanger<T> object permits two threads to exchange values of type T.
  * If thread A calls the object’s exchange() method with argument a,
  * and B calls the same object’s exchange() method with argument b,
  * then A’s call will return value b and vice versa.
  */
class ElmininationStack {

}
